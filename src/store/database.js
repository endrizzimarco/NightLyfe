import { firebaseAuth, firebaseDb } from 'boot/firebase'

const state = {}
const mutations = {}
const actions = {
  registerUser() {
    console.log('registerUser')
  }
}
const getters = {}

export default {
  state,
  mutations,
  actions,
  getters
}